import * as TU from '@utility/Theme.utility';

const PRIMARY = "#3A3A3A";
const DARK    = "#8AA0BA";
const LIGHT   = "#C5D0DD";

export default TU.createTheme("Platform Admin", PRIMARY, DARK, LIGHT);
